export interface IRoom {
    id: string;
    number: number;
    name: string;
    participants: number;
    maxParticipants: number;
}

export interface IRoomDetails {
    id: string;
    number: number;
    name: string;
    participants: number;
    maxParticipants: number;
}
